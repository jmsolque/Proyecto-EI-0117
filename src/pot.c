#include "pot.h"
#include <gtk/gtk.h>
#include <stdlib.h>

//  variables globales
GtkWidget *entry_voltaje;
GtkWidget *entry_corriente;
GtkWidget *entry_resistencia;
GtkWidget *combo_formula;
GtkWidget *label_resultado;

// Función que calcula la potencia 
void calcular_potencia(GtkWidget *widget, gpointer data) {
    
}

// Limpia los campos
void limpiar_campos_pot(GtkWidget *widget, gpointer data) {
    
}

// Para calcular cuando se presione enter
void enter_calcular_pot(GtkWidget *entry, gpointer data) {
   
}
